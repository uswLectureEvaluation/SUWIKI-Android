<resources>
    <declare-styleable name="AppWidgetAttrs">
        <attr name="appWidgetBackgroundColor" format="color" />
        <attr name="appWidgetTextColor" format="color" />
    </declare-styleable>

    <declare-styleable name="UswTimeTable">
        <attr name="isEmpty" format="boolean" />
        <attr name="infoFormat" format="integer" />
        <!-- ... -->
    </declare-styleable>

    <declare-styleable name="FlexBoxRadioGroup">
        <!-- The id of the child radio button that should be checked by default
             within this radio group. -->
        <attr name="uiCheckedButton" format="integer" />
    </declare-styleable>
</resources>