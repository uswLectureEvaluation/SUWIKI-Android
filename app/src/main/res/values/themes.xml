<resources>
    <!-- Base application theme. -->
    <style name="Theme.Uswtimetable" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/suwiki_blue_900</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/suwiki_blue_900</item>
        <item name="colorSecondaryVariant">@color/purple_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="errorTextColor">@color/suwiki_purple</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/background</item>
        <item name="bottomSheetDialogTheme">@style/AppBottomSheetDialogTheme</item>
        <item name="appBarLayoutStyle">@style/SuwikiAppbar</item>
    </style>

    <!-- Splash 화면 테마 -->
    <style name="Theme.Uswtimetable.Splash">
        <item name="android:statusBarColor">@color/background_start</item>
        <item name="android:windowBackground">@color/background_start</item>
    </style>

    <style name="ThemeOverlay.Uswtimetable.AppWidgetContainer" parent="">
        <item name="appWidgetBackgroundColor">@color/light_blue_600</item>
        <item name="appWidgetTextColor">@color/light_blue_50</item>
    </style>

    <style name="AppBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="background">@drawable/bottom_sheet_background</item>
    </style>

    <style name="SuwikiAppbar" parent="Widget.AppCompat.Toolbar">
        <item name="background">@color/background</item>
    </style>

    <style name="Theme.Uswtimetable.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Uswtimetable.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Uswtimetable.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>